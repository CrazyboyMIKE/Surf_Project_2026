# Fifth-Round Real Environment Readiness Report

日期：2026-07-10（Asia/Shanghai）

## 1. Scope

本轮只补齐前四轮真实验收准备，不开发第五轮产品功能：

- 不做语音通话。
- 不做真实机器人运动控制。
- 不调用厂商导航 SDK。
- 不接 MQTT 控制真实机器人。
- 不新增数据库或账号系统。
- 不提交或打印任何真实 secret。

## 2. Fixed Readiness Gaps

已补强的前四轮验收缺口：

- `docs/FOUR_ROUND_FULL_TEST_PLAN.md` 已存在，并补充真实 LiveKit、Android 摄像头、线上 HTTPS/WSS 的证据采集要求。
- `docs/ANDROID_ROBOT_SETUP.md` 补充 Android Studio、Android SDK Platform 35/27、`ANDROID_HOME`、Platform-Tools、`adb`、Gradle wrapper、`./gradlew assembleDebug`、`adb install` 的可复现步骤。
- `android-robot/README.md` 补充 API 27、Android SDK、Platform-Tools、Gradle/Wrapper 的构建前提。
- `docs/LIVEKIT_SETUP.md` 补充如何配置真实 LiveKit 凭证、如何通过 `/api/robots/join` 确认 backend 已从 mock token mode 切换为 real token mode、如何避免在检查时打印 token。
- `docs/DEPLOYMENT.md` 补充 backend/web 部署 readiness gates，包括 `/health`、`VITE_API_BASE_URL=https://...`、`VITE_WS_BASE_URL=wss://...`、`CORS_ORIGIN`、WebSocket upgrade 和日志不打印 secret。

## 3. Static Verification

Android project structure:

- `android-robot/settings.gradle` exists.
- `android-robot/build.gradle` exists.
- `android-robot/app/build.gradle` exists.
- `android-robot/app/src/main/AndroidManifest.xml` exists.
- `minSdk 27` is configured.
- Manifest includes `INTERNET`, `CAMERA`, and optional `RECORD_AUDIO`.
- Manifest keeps `android:usesCleartextTraffic="true"` for LAN debugging; docs state online tests must use trusted `https://`.
- Android Studio app exists at `/Applications/Android Studio.app`.
- Android SDK directory exists at `/Users/linziwei/Library/Android/sdk`.
- ADB exists at `/Users/linziwei/Library/Android/sdk/platform-tools/adb`, but `adb` is not currently on `PATH`.
- `ANDROID_HOME` is not currently set in this shell.
- Installed SDK platforms observed in this shell include `android-36.1`; `android-35` and `android-27` were not observed and should be installed for this project.
- System `gradle` is not currently available.
- No generated `android-robot/gradlew` wrapper is present.

LiveKit path:

- Backend `.env.example` contains `LIVEKIT_URL`, `LIVEKIT_API_KEY`, and `LIVEKIT_API_SECRET` placeholders.
- Backend generates tokens through `backend/src/services/liveKitTokenService.ts`.
- Web client connects to LiveKit with backend-returned `liveKitUrl` and `token`.
- Robot web publisher calls `POST /api/robots/join`, requests browser camera permission in real token mode, and publishes a camera track.
- Android robot calls `POST /api/robots/join`, uses the returned token, and does not contain LiveKit API secret.

Robot video rendering:

- `web-client/src/useLiveKitRoom.ts` subscribes to remote tracks.
- `web-client/src/components/RobotVideo.tsx` prefers participant identity/name containing `robot`.
- Robot offline state displays `Robot offline`.
- Robot online with LiveKit connected but no track displays `Waiting for robot video`.

Security:

- No `.env`, `.env.local`, or `.env.production` files were found.
- `.env.example` files use placeholders only.
- Web and Android source do not contain `LIVEKIT_API_SECRET` values.
- Backend request logs do not print LiveKit tokens or secrets.
- Android and Web receive tokens from backend only.

## 4. Commands Run

Backend:

```bash
cd backend
npm run lint
npm run test
npm run build
```

Result:

- `npm run lint`: passed.
- `npm run test`: passed, including `commandValidation tests passed`.
- `npm run build`: passed.

Web client:

```bash
cd web-client
npm run lint
npm run test
npm run build
```

Result:

- `npm run lint`: passed.
- `npm run test`: passed.
- `npm run build`: passed.
- Vite reported a non-blocking chunk size warning from the LiveKit client bundle.

Robot web publisher:

```bash
cd robot-web-publisher
npm run lint
npm run test
npm run build
```

Result:

- `npm run lint`: passed.
- `npm run test`: passed.
- `npm run build`: passed.
- Vite reported a non-blocking chunk size warning from the LiveKit client bundle.

Android robot:

```bash
cd android-robot
./gradlew test
./gradlew assembleDebug
```

Result:

- `./gradlew test`: failed because `./gradlew` does not exist.
- `./gradlew assembleDebug`: failed because `./gradlew` does not exist.
- `gradle wrapper`: failed because system `gradle` is not installed.
- `command -v adb`: no `adb` found on `PATH`.
- `/Users/linziwei/Library/Android/sdk/platform-tools/adb version`: passed, so Platform-Tools exist but must be added to `PATH`.
- `ANDROID_HOME`: not set in the current shell.
- Android SDK exists, but required `platforms/android-35` and `platforms/android-27` were not observed.

No Gradle wrapper was generated because this environment does not have Gradle. The wrapper must be generated by Gradle or Android Studio, not hand-written.

Safety/static checks:

```bash
find . -name .env -o -name .env.local -o -name .env.production
git diff --check
rg -n --glob '!**/node_modules/**' --glob '!**/dist/**' --glob '!**/.git/**' "LIVEKIT_API_SECRET|LIVEKIT_API_KEY|eyJ[A-Za-z0-9_-]{20,}|BEGIN PRIVATE KEY|password|token" backend web-client robot-web-publisher android-robot docs README.md
```

Result:

- No `.env`, `.env.local`, or `.env.production` files were found.
- `git diff --check` passed.
- Secret scan found only placeholders, variable names, token field names, and expected documentation references; no real secret/JWT/private key was found.

## 5. Real Environment Items Not Yet Verified

These items still require real external environment:

- Real LiveKit Cloud credentials:
  - `LIVEKIT_URL`
  - `LIVEKIT_API_KEY`
  - `LIVEKIT_API_SECRET`
- Browser camera permission for `robot-web-publisher`.
- Android SDK Platform 35 and Platform 27 installation.
- `ANDROID_HOME` / `ANDROID_SDK_ROOT` shell configuration.
- Platform-Tools added to `PATH`.
- Gradle or generated Gradle wrapper.
- Android 8.1/API 27+ robot or test phone.
- Public backend deployment with HTTPS/WSS.
- Public web-client deployment with HTTPS.
- Hosting provider WebSocket upgrade support.
- Real CORS validation against deployed Web origin.

## 6. Can Real Tests Start?

Real LiveKit test:

- Status: ready after the operator adds real LiveKit values to local `backend/.env`.
- Recommended first test: `robot-web-publisher` browser camera test.

Android robot true-device test:

- Status: blocked in the current shell until Gradle/wrapper, required SDK platforms, PATH/ANDROID_HOME setup, and a device are available.
- Project structure is ready for Android Studio import.

Public deployment test:

- Status: ready from a documentation/configuration perspective, but not executed.
- Requires backend and web hosting accounts/domains with HTTPS/WSS.

## 7. Next Real Test Order

1. Create a LiveKit Cloud project.
2. Put `LIVEKIT_URL`, `LIVEKIT_API_KEY`, and `LIVEKIT_API_SECRET` into local `backend/.env`.
3. Start backend and confirm `LiveKit token mode: livekit`.
4. Start `web-client`.
5. Start `robot-web-publisher`.
6. Join the same room from Web and robot publisher.
7. Confirm Web shows robot publisher camera video.
8. Install Android Studio or Android SDK command-line tools.
9. Open `android-robot/` in Android Studio and sync.
10. Install Android SDK Platform 27, compile SDK, Build-Tools, and Platform-Tools.
11. Generate Gradle wrapper with Gradle or Android Studio if needed.
12. Run `./gradlew test` and `./gradlew assembleDebug`.
13. Install APK with `adb install -r app/build/outputs/apk/debug/app-debug.apk`.
14. Android robot uses `backendUrl=http://<computer-LAN-IP>:3001` for LAN test.
15. Web joins the same `roomName` and confirms Android camera video.
16. Deploy backend with HTTPS/WSS and LiveKit env.
17. Deploy Web with `VITE_API_BASE_URL=https://...` and `VITE_WS_BASE_URL=wss://...`.
18. Android robot uses `backendUrl=https://your-backend.example.com`.
19. External users A and B validate video, chat, controller/viewer permissions, and mock-only control receipt.

## 8. Formal Acceptance Blockers

Still blocking full formal real-environment acceptance:

- No real LiveKit credentials were provided in this workspace.
- No Android SDK, Gradle, ADB, or Android robot device is available in this environment.
- No public backend/web deployment exists yet.
- No real HTTPS/WSS/CORS online validation has been executed.

These blockers are environmental, not newly introduced code blockers.
